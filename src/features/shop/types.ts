export type Product = {
  id: string;
  title: string;
  price: number;
  code: string;    // код товару
  image: string;   // шлях до картинки 1x
  image2x: string; // шлях до картинки 2x (ретина)
};